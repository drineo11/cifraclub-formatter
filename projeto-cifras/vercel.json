{
  "buildCommand": "npm run build",
  "installCommand": "npm install && pip install -r requirements.txt",
  "framework": "nextjs",
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "includeFiles": "lib/**"
    }
  }
}