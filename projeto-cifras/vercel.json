{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "functions": {
    "api/generate.py": {
      "maxDuration": 60,
      "includeFiles": "lib/**"
    }
  }
}